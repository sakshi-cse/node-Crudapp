version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/node-Crudapp
    overwrite: true
hooks:
  ApplicationStart:
    - location: scripts/start_application.sh
      timeout: 300
      runas: root
